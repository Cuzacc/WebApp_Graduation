# Audit Report - 2026-04-21 (Quick Scan)

## Summary
- 🔴 Critical Issues: 1
- 🟡 Warnings: 3
- 🟢 Suggestions: 2

---

## 🔴 Critical Issues (Phải sửa ngay)
1. **Thiếu ràng buộc dữ liệu đầu vào (Input Validation)**
   - **Vị trí**: [schemas.py](file:///f:/DevOps/web/Graduation/backend/schemas.py)
   - **Nguy hiểm**: Người dùng có thể gửi hàng GB dữ liệu hoặc đính kèm mã độc vào ô "Lời chúc" hoặc "Tên". Điều này có thể làm đầy ổ đĩa, treo server (DoS) hoặc làm hỏng giao diện hiển thị.
   - **Cách sửa**: Thêm `max_length` cho các trường chuỗi và giới hạn số lượng người tham dự (`attendees_count`).

---

## 🟡 Warnings (Nên sửa)
1. **Chưa có file cấu hình môi trường chuẩn (.env)**
   - **Triệu chứng**: Hệ thống đang dùng dữ liệu "phòng hờ" (fallback) hoặc ví dụ từ `.env.example`.
   - **Hậu quả**: Nếu anh đưa lên server thật mà quên tạo file này, mật khẩu thư ngầm (SMTP) sẽ không chạy, hoặc Token Admin sẽ cực kỳ dễ bị hack vì dùng mật khẩu mặc định.
2. **CORS cấu hình cứng cho Localhost**
   - **Vị trí**: [main.py:17](file:///f:/DevOps/web/Graduation/backend/main.py#L17)
   - **Hậu quả**: Khi anh đóng gói Docker và triển khai lên mạng, giao diện Web sẽ có thể bị Server từ chối vì nó chỉ đang "chơi" duy nhất với cái tên `localhost:3000`.
3. **Mã hóa Token có thời hạn quá dài**
   - **Vị trí**: [auth.py:14](file:///f:/DevOps/web/Graduation/backend/auth.py#L14)
   - **Hậu quả**: Token đăng nhập Admin kéo dài tới 24 tiếng. Nếu lỡ bị lộ Token, kẻ xấu có toàn quyền quậy phá Dashboard của anh trong suốt một ngày trời.

---

## 🟢 Suggestions (Tùy chọn)
1. **Cập nhật hàm thời gian**: Dùng `timezone.utc` thay cho `utcnow()` đang bị lỗi thời trong Python mới.
2. **Thêm Metadata cho API**: Bổ sung thêm ví dụ (Example) cho các API để sau này người khác nhìn vào dễ dùng hơn.

---

## Next Steps
Anh nên ưu tiên sửa ngay mục 🔴 để bảo mật cho dự án trước khi chúng ta "nhốt" nó vào lồng Docker.
